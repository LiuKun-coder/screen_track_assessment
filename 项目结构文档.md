# 校园电动车违规筛查与轨迹管理系统 — 项目结构文档

> **项目名称**：Screen Track Assessment（校园电动车智能违规监控平台）  
> **项目性质**：大学生创新创业项目  
> **文档更新日期**：2026年3月2日  
> **仓库结构**：Monorepo（单仓库多模块）

---

## 一、项目概述

本系统旨在对校园内电动车违规行为进行智能化管理，涵盖违规记录、轨迹回溯、电子围栏、申诉处理和数据可视化大屏等核心功能。系统采用前后端分离架构，包含三个主要模块：

| 模块 | 技术栈 | 说明 |
|------|--------|------|
| **backend** | Spring Boot 3.2 + MyBatis-Plus + MySQL | RESTful API 后端服务 |
| **frontend-web** | Vue 3 + Element Plus + ECharts + 高德地图 | Web 管理端（PC 浏览器） |
| **frontend-mobile** | UniApp (Vue 3) + Pinia | 移动端（微信小程序 / App / H5） |

---

## 二、顶层目录结构

```
screen_track_assessment/
│
├── backend/                    # 后端服务 (Spring Boot)
├── frontend-web/               # Web 前端 (Vue 3 + Element Plus)
├── frontend-mobile/            # 移动端前端 (UniApp)
│
├── README.md                   # 项目总说明
├── CONTRIBUTING.md             # 团队协作开发规范
├── 未实现功能清单.md            # 待开发功能清单与方案
└── 后续手动操作指引.md          # 手动操作指引文档
```

---

## 三、后端模块 (`backend/`)

### 3.1 技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.2.5 | Web 框架 |
| MyBatis-Plus | 3.5.5 | ORM 框架（含分页插件、自动填充） |
| MySQL | 8.0+ | 关系型数据库 |
| JWT (jjwt) | 0.9.1 | 用户认证（Token 有效期 24h） |
| Spring Security Crypto | — | 密码加密（BCrypt） |
| FastJson2 | 2.0.43 | JSON 序列化/反序列化 |
| 阿里云 SMS | 2.0.24 | 短信验证码服务 |
| Lombok | — | 简化实体类代码 |

### 3.2 后端目录结构

```
backend/
├── pom.xml                             # Maven 依赖管理
├── sql/
│   └── init_schema.sql                 # 数据库初始化脚本
├── add_admin.sql                       # 管理员初始化 SQL
├── init_test_data.sql                  # 测试数据 SQL
├── mvnw / mvnw.cmd                     # Maven Wrapper
│
└── src/
    ├── main/
    │   ├── java/org/example/sc_backend/
    │   │   ├── ScBackendApplication.java           # Spring Boot 启动类
    │   │   │
    │   │   ├── common/                             # 公共模块
    │   │   │   ├── config/
    │   │   │   │   ├── MybatisPlusConfig.java      # MyBatis-Plus 配置（分页插件 + createTime 自动填充）
    │   │   │   │   └── WebMvcConfig.java           # WebMvc 配置（CORS、JWT 拦截器、静态资源映射）
    │   │   │   ├── entity/
    │   │   │   │   └── BaseEntity.java             # 实体基类（id, createTime, deleted）
    │   │   │   ├── exception/
    │   │   │   │   ├── BusinessException.java      # 自定义业务异常（code + message）
    │   │   │   │   └── GlobalExceptionHandler.java # 全局异常处理器（@RestControllerAdvice）
    │   │   │   ├── interceptor/
    │   │   │   │   └── JwtInterceptor.java         # JWT 拦截器（解析 Token → 注入 userId/role）
    │   │   │   └── utils/
    │   │   │       ├── AdminAuthUtils.java         # 管理员权限校验工具
    │   │   │       ├── JwtUtils.java               # JWT 工具类（生成/解析/验证 Token）
    │   │   │       └── Result.java                 # 统一响应体 {code, message, data, timestamp}
    │   │   │
    │   │   ├── controller/                         # 控制器层（8 个）
    │   │   │   ├── AuthController.java             # 认证（登录/注册/重置密码/验证码）
    │   │   │   ├── ViolationController.java        # 违规管理
    │   │   │   ├── AppealController.java           # 申诉管理
    │   │   │   ├── TrackController.java            # 轨迹管理
    │   │   │   ├── FenceController.java            # 电子围栏管理
    │   │   │   ├── UserController.java             # 用户管理
    │   │   │   ├── FileController.java             # 文件上传
    │   │   │   └── ScreenController.java           # 大屏统计数据
    │   │   │
    │   │   ├── dto/                                # 数据传输对象（11 个）
    │   │   │   ├── LoginDTO.java                   # 登录请求体
    │   │   │   ├── LoginVO.java                    # 登录响应体（token + userInfo）
    │   │   │   ├── RegisterDTO.java                # 注册请求体
    │   │   │   ├── ResetPasswordDTO.java           # 重置密码请求体
    │   │   │   ├── AppealSubmitDTO.java            # 申诉提交请求体
    │   │   │   ├── TrackUploadDTO.java             # 轨迹上传请求体（含内部类 TrackPointDTO）
    │   │   │   ├── ViolationQueryDTO.java          # 违规查询参数
    │   │   │   ├── ViolationVO.java                # 违规记录视图对象
    │   │   │   ├── UserQueryDTO.java               # 用户查询参数
    │   │   │   ├── UserVO.java                     # 用户信息视图对象（脱敏）
    │   │   │   └── ScreenStatsVO.java              # 大屏统计响应体
    │   │   │
    │   │   ├── entity/                             # 数据库实体类（6 个）
    │   │   │   ├── SysUser.java                    # 系统用户（学生/教师/职工）
    │   │   │   ├── SysAdmin.java                   # 管理员
    │   │   │   ├── BizViolation.java               # 违规记录
    │   │   │   ├── BizAppeal.java                  # 申诉记录
    │   │   │   ├── BizFence.java                   # 电子围栏
    │   │   │   └── BizTrack.java                   # 轨迹记录
    │   │   │
    │   │   ├── mapper/                             # MyBatis Mapper 接口（6 个）
    │   │   │   ├── SysUserMapper.java              # 用户 Mapper
    │   │   │   ├── SysAdminMapper.java             # 管理员 Mapper
    │   │   │   ├── BizViolationMapper.java         # 违规 Mapper（含自定义统计 SQL）
    │   │   │   ├── BizAppealMapper.java            # 申诉 Mapper
    │   │   │   ├── BizFenceMapper.java             # 围栏 Mapper
    │   │   │   └── BizTrackMapper.java             # 轨迹 Mapper
    │   │   │
    │   │   └── service/                            # 服务层（8 个接口 + 8 个实现）
    │   │       ├── AuthService.java                # 认证服务接口
    │   │       ├── ViolationService.java           # 违规管理服务接口
    │   │       ├── AppealService.java              # 申诉服务接口
    │   │       ├── TrackService.java               # 轨迹服务接口
    │   │       ├── FenceService.java               # 围栏服务接口
    │   │       ├── UserService.java                # 用户管理服务接口
    │   │       ├── ScreenService.java              # 大屏统计服务接口
    │   │       ├── SmsCodeService.java             # 短信验证码服务接口
    │   │       └── impl/
    │   │           ├── AuthServiceImpl.java        # 认证实现（BCrypt 加密、双表登录）
    │   │           ├── ViolationServiceImpl.java   # 违规实现（动态查询、JSON 解析证据图）
    │   │           ├── AppealServiceImpl.java      # 申诉实现（事务、自动生成申诉编号）
    │   │           ├── TrackServiceImpl.java       # 轨迹实现（Haversine 距离计算、同日合并）
    │   │           ├── FenceServiceImpl.java       # 围栏实现（软删除 = status 改 deleted）
    │   │           ├── UserServiceImpl.java        # 用户管理实现
    │   │           ├── ScreenServiceImpl.java      # 大屏统计实现（聚合违规 + 申诉数据）
    │   │           └── SmsCodeServiceImpl.java     # 短信验证码实现（阿里云 SMS + 内存缓存）
    │   │
    │   └── resources/
    │       ├── application.yml                     # 应用配置文件
    │       ├── static/                             # 静态资源目录
    │       └── templates/                          # 模板目录
    │
    └── test/                                       # 单元测试
        └── java/org/...
```

### 3.3 数据库设计（6 张核心表）

| 表名 | 用途 | 核心字段 |
|------|------|----------|
| `sys_user` | 系统用户（学生/教师/职工） | id, username(学号), password(BCrypt), name, phone, email, avatar, department, user_type, status, deleted |
| `sys_admin` | 管理员 | id, username, password, name, phone, role(admin/super_admin), department, deleted |
| `biz_violation` | 违规记录 | id, user_id, violation_time, place, type(超速/违停/逆行/闯红灯), detail, penalty, status(pending/processed), evidence_images(JSON), can_appeal, appeal_status, deleted |
| `biz_appeal` | 申诉记录 | id, violation_id(FK), user_id, appeal_number(唯一), platform(web/miniapp), reason, description, evidence_files(JSON), status(pending/approved/rejected), reply_content, handler_id, handle_time |
| `biz_fence` | 电子围栏 | id, name, range_desc, coordinates(JSON 坐标点集), status(active/inactive/deleted) |
| `biz_track` | 日轨迹记录 | id, user_id, date, start_time, end_time, distance(km), track_points(JSON 大字段: time/lat/lng/speed) |

### 3.4 应用配置摘要

| 配置项 | 值 |
|--------|-----|
| 服务端口 | `8080`，路径前缀 `/api` |
| 数据库 | MySQL `localhost:3306/scs_db` |
| 文件上传限制 | 单文件 50MB |
| JWT 有效期 | 24h (86400000ms) |
| 短信验证码 | 阿里云 SMS，有效 300s，重发间隔 60s |
| CORS | 全放开 (`allowedOriginPatterns("*")`) |

### 3.5 API 接口清单

所有接口基础路径：**`/api`**

#### 3.5.1 认证模块 `/auth` — AuthController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/auth/login` | 用户/管理员登录 | 无 |
| POST | `/auth/register` | 用户注册 | 无 |
| POST | `/auth/reset-password` | 重置密码（短信验证） | 无 |
| POST | `/auth/sendCode` | 发送短信验证码 | 无 |
| POST | `/auth/refresh-token` | 刷新 Token | 无 |
| POST | `/auth/logout` | 退出登录 | 需 Token |
| GET | `/auth/userInfo` | 获取当前用户信息 | 需 Token |

#### 3.5.2 违规管理 `/violations` — ViolationController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/violations/reports` | 违规列表（分页/筛选/搜索） | 需 Token |
| GET | `/violations/statistics` | 违规统计（总数/我的/待处理） | 需 Token |
| GET | `/violations/my` | 当前用户违规记录 | 需 Token |
| GET | `/violations/{id}` | 违规详情 | 需 Token |
| POST | `/violations` | 添加违规记录 | 管理员 |
| PUT | `/violations/{id}` | 更新违规记录 | 管理员 |

#### 3.5.3 申诉管理 `/appeals` — AppealController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/appeals` | 提交申诉 | 需 Token |
| GET | `/appeals/my` | 我的申诉列表 | 需 Token |
| GET | `/appeals/{id}` | 申诉详情 | 需 Token |
| GET | `/appeals/list` | 所有申诉列表 | 管理员 |
| PUT | `/appeals/{id}/handle` | 处理申诉（审批/驳回） | 管理员 |

#### 3.5.4 轨迹管理 `/tracks` — TrackController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/tracks/byDate` | 按日期查询轨迹 | 需 Token |
| GET | `/tracks/list` | 用户轨迹列表（分页） | 需 Token |
| GET | `/tracks/{id}` | 轨迹详情 | 需 Token |
| POST | `/tracks/upload` | 上传轨迹点（同日合并） | 需 Token |

#### 3.5.5 电子围栏 `/fences` — FenceController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/fences` | 获取所有活跃围栏 | 需 Token |
| GET | `/fences/{id}` | 围栏详情 | 需 Token |
| POST | `/fences` | 添加围栏 | 管理员 |
| PUT | `/fences/{id}` | 更新围栏 | 管理员 |
| DELETE | `/fences/{id}` | 删除围栏（软删除） | 管理员 |

#### 3.5.6 用户管理 `/users` — UserController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/users/list` | 用户列表（分页+筛选） | 管理员 |
| GET | `/users/{id}` | 用户详情 | 管理员 |
| PUT | `/users/{id}` | 更新用户信息 | 管理员 |
| PUT | `/users/{id}/status` | 启用/禁用用户 | 管理员 |
| GET | `/users/search` | 搜索用户 | 管理员 |
| PUT | `/users/profile` | 用户修改个人资料 | 需 Token |

#### 3.5.7 文件上传 `/upload` — FileController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/upload` | 上传文件（按日期分目录存储） | 无 |

#### 3.5.8 大屏统计 `/screen` — ScreenController

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/screen/stats` | 大屏统计数据（违规趋势、类型分布等） | 需 Token |

### 3.6 关键业务逻辑

1. **双表登录**：`AuthServiceImpl` 先查 `sys_admin` 表，再查 `sys_user` 表；密码支持 BCrypt 和明文兼容（首次明文登录后自动升级为 BCrypt）
2. **轨迹合并**：同一用户同一天多次上传轨迹时自动合并，使用 **Haversine 公式**计算里程
3. **申诉事务**：提交申诉时在事务内同步更新 `biz_violation.appeal_status`
4. **SMS 验证码**：基于阿里云 SMS + 内存 `ConcurrentHashMap` 缓存，支持发送频率限制
5. **权限控制**：通过 `AdminAuthUtils.requireAdmin()` 在 Controller 层手动校验管理员角色

---

## 四、Web 前端模块 (`frontend-web/`)

### 4.1 技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.2.13 | 核心框架 |
| Vue Router | 4.5.1 | 路由管理（History 模式） |
| Element Plus | 2.10.1 | UI 组件库 |
| Axios | 1.13.4 | HTTP 请求 |
| ECharts | 6.0.0 | 数据可视化图表 |
| 高德地图 JS API | 1.0.1 | 轨迹地图展示 |
| Vue CLI | 5.0 | 构建工具（开发端口 8081） |

### 4.2 Web 前端目录结构

```
frontend-web/
├── package.json                        # 依赖管理
├── vue.config.js                       # Vue CLI 构建配置（端口 8081）
├── babel.config.js                     # Babel 配置
├── jsconfig.json                       # JS 路径别名
├── public/
│   └── index.html                      # HTML 模板
│
└── src/
    ├── main.js                         # 应用入口（注册 Element Plus + Router）
    ├── App.vue                         # 根组件（<router-view /> + 全局字体样式）
    │
    ├── api/                            # API 接口模块（8 个）
    │   ├── auth.js                     # 认证 API（登录/注册/验证码/资料更新）
    │   ├── violation.js                # 违规 API（列表/详情/统计/增改）
    │   ├── appeal.js                   # 申诉 API（提交/列表/详情/处理）
    │   ├── track.js                    # 轨迹 API（按日期查/列表/详情）
    │   ├── fence.js                    # 围栏 API（CRUD）
    │   ├── user.js                     # 用户管理 API（列表/详情/状态/搜索）
    │   ├── screen.js                   # 大屏统计 API
    │   └── upload.js                   # 文件上传 API
    │
    ├── views/                          # 页面视图（5 个）
    │   ├── Login.vue                   # 登录页（账号密码 + 扫码登录）
    │   ├── Register.vue                # 注册页（手机号 + 验证码）
    │   ├── ResetPassword.vue           # 重置密码页
    │   ├── Home.vue                    # 主框架页（侧边栏菜单 + 内容区）
    │   └── ViolationScreenPage.vue     # 违规大屏独立页面
    │
    ├── components/                     # 业务组件（8 个）
    │   ├── AdminManagement.vue         # 用户/管理员管理
    │   ├── ViolationReport.vue         # 违规通报公告大厅
    │   ├── MyViolations.vue            # 我的违规记录
    │   ├── TrajectoryTracking.vue      # 轨迹回溯 + 电子围栏（高德地图）
    │   ├── Appeal.vue                  # 用户申诉提交（三步骤流程）
    │   ├── AppealComplaintHandler.vue  # 管理员申诉处理工作台
    │   ├── UserProfile.vue             # 个人信息与设置
    │   └── ViolationScreen.vue         # 违规监控数据可视化大屏
    │
    ├── router/
    │   └── index.js                    # 路由配置 + 导航守卫
    │
    ├── utils/
    │   └── request.js                  # Axios 封装（基础 URL、Token 注入、401 处理）
    │
    └── assets/                         # 静态资源
        ├── main.css                    # 全局样式（温暖自然主题 CSS 变量）
        ├── cumt_nanhu.json             # 中国矿业大学南湖校区 GeoJSON 地图
        └── tongshan.json               # 铜山区 GeoJSON 地图数据
```

### 4.3 路由结构

| 路径 | 名称 | 组件 | 认证要求 |
|------|------|------|----------|
| `/` | Login | Login.vue | 无 |
| `/register` | Register | Register.vue | 无 |
| `/reset-password` | ResetPassword | ResetPassword.vue | 无 |
| `/home` | Home | Home.vue | 需登录 |
| `/violation-screen` | ViolationScreen | ViolationScreenPage.vue | 需登录 |

**路由守卫**：检查 `localStorage` 中的 `token`/`userInfo`，未登录时重定向到 `/`，非管理员无法访问管理页面。

### 4.4 页面 / 组件功能详述

#### 视图层 (Views)

| 视图 | 功能说明 |
|------|----------|
| **Login.vue** | 支持账号密码登录和扫码登录 Tab 切换；内置测试账号快速填入；登录成功保存 token 到 localStorage 后跳转 `/home` |
| **Register.vue** | 手机号 + 短信验证码注册；60 秒倒计时；手机号格式验证、密码一致性验证 |
| **ResetPassword.vue** | 手机号 + 验证码验证身份后重置密码 |
| **Home.vue** | 经典左侧边栏 + 右内容区布局；**角色菜单过滤**：管理员可见全部 8 个菜单，普通用户可见 6 个；支持组件间事件通信 |
| **ViolationScreenPage.vue** | 全屏包装器，`100vh` 展示 `ViolationScreen` 组件 |

#### 业务组件 (Components)

| 组件 | 功能说明 |
|------|----------|
| **AdminManagement.vue** | 用户列表展示（头像、姓名、手机、类型）；支持筛选/新增/编辑/删除；分页（每页 4 条）；仅管理员可见 |
| **ViolationReport.vue** | 违规通报公告列表；三维度筛选（方式/地点/时间）；分页（每页 5 条） |
| **MyViolations.vue** | 当前用户违规卡片列表；支持筛选 + 查看详情对话框；可一键跳转轨迹回溯 |
| **TrajectoryTracking.vue** | 🔑 核心组件（1197 行）；**高德地图集成**：轨迹绘制、按日期查看历史轨迹；管理员可查指定用户轨迹；**电子围栏管理**（查看/编辑）；违规信息详情面板 |
| **Appeal.vue** | 三步骤申诉流程：选择违规记录 → 填写申诉信息（理由/描述/证据上传） → 确认提交 |
| **AppealComplaintHandler.vue** | 🔑 核心组件（1697 行）；管理员申诉处理工作台；统计概览 + 状态筛选 + 搜索；批量处理功能；状态流转（待处理 → 处理中 → 已完成） |
| **UserProfile.vue** | 双栏布局：信息卡片 + 操作面板；头像上传；行内编辑个人信息；修改密码；使用统计（登录天数/违规次数/安全评分）；退出登录 |
| **ViolationScreen.vue** | 🔑 核心组件（1012 行）；全屏深色大屏；KPI 指标卡（今日违规/月累计/处理率/在线设备）；**ECharts 图表**：饼图（类型分布）、折线图（7 日趋势）、柱状图（区域排行）；**校园地图**（基于 GeoJSON）；实时报警滚动列表 |

---

## 五、移动端模块 (`frontend-mobile/`)

### 5.1 技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| UniApp (DCloudio) | 3.0.0-406 | 跨平台框架 |
| Vue | 3.4.21 | 核心框架（Composition API） |
| Pinia | 3.0.4 | 状态管理 |
| vue-i18n | 9.1.9 | 国际化（预留） |
| Vite | — | 构建工具 |

**支持平台**：微信小程序、支付宝小程序、App (Android/iOS)、H5 等

### 5.2 移动端目录结构

```
frontend-mobile/
├── package.json                        # 依赖管理
├── vite.config.js                      # Vite 构建配置
├── index.html                          # HTML 模板
│
└── src/
    ├── App.vue                         # 应用入口（生命周期管理 + 全局 SCSS 样式）
    ├── main.js                         # Vue 3 SSR 入口（挂载 Pinia）
    ├── pages.json                      # 页面路由 + TabBar 配置
    ├── manifest.json                   # 平台编译配置
    ├── uni.scss                        # 全局 SCSS 变量
    │
    ├── api/                            # API 接口模块（6 个）
    │   ├── auth.js                     # 认证 API（9 个方法，含微信登录）
    │   ├── violation.js                # 违规 API（4 个方法 + 2 个数据适配函数）
    │   ├── appeal.js                   # 申诉 API（3 个方法，自动标记 platform='miniapp'）
    │   ├── track.js                    # 轨迹 API（5 个方法）
    │   ├── fence.js                    # 围栏 API（2 个方法）
    │   └── upload.js                   # 文件上传 API（使用 uni.uploadFile 原生上传）
    │
    ├── config/                         # 配置模块（3 个）
    │   ├── index.js                    # 基础配置（BASE_URL, TIMEOUT, DEBUG, VERSION）
    │   ├── api.js                      # API 端点路径常量集中定义
    │   └── constant.js                 # 业务常量（违规类型/状态/地点/时间选项）
    │
    ├── stores/                         # Pinia 状态管理（4 个）
    │   ├── index.js                    # Pinia 入口
    │   ├── user.js                     # 用户状态（登录/注册/Token/个人信息）
    │   ├── system.js                   # 系统状态（设备信息/网络/导航栏高度/Loading）
    │   └── violation.js                # 违规状态（列表/筛选/分页）
    │
    ├── utils/                          # 工具模块（7 个）
    │   ├── request.js                  # HTTP 请求封装（uni.request + 拦截器链）
    │   ├── auth.js                     # 认证工具（Token 存取/登录状态/重定向）
    │   ├── route-guard.js              # 路由守卫（白名单页面免登录）
    │   ├── date.js                     # 日期格式化 + 相对时间 + 时间范围
    │   ├── json-parser.js              # JSON 安全解析（证据图片/轨迹点/围栏坐标）
    │   ├── storage.js                  # 本地存储封装（支持 TTL 过期机制）
    │   └── validate.js                 # 表单验证（手机号/邮箱/密码/用户名/车牌号）
    │
    ├── components/business/            # 可复用业务组件（3 个，easycom 自动注册）
    │   ├── violation-card.vue          # 违规卡片（类型/描述/地点/时间/状态标签）
    │   ├── filter-panel.vue            # 侧滑筛选面板（时间/地点/类型/状态）
    │   └── map-track.vue               # 地图轨迹组件（polyline 轨迹线/标记点/控制按钮）
    │
    ├── pages/                          # 页面（7 大模块，19 个页面）
    │   ├── auth/                       # 认证模块
    │   ├── home/                       # 首页模块
    │   ├── violation/                  # 违规模块
    │   ├── track/                      # 轨迹模块
    │   ├── appeal/                     # 申诉模块
    │   ├── profile/                    # 个人中心模块
    │   └── index/                      # 默认页（未使用）
    │
    └── static/
        └── icons/                      # 图标资源
```

### 5.3 页面路由与 TabBar

#### TabBar 导航（底部 4 个入口）

| Tab | 路径 | 标题 |
|-----|------|------|
| 🏠 首页 | `pages/home/index` | 首页 |
| ⚠️ 违规查询 | `pages/violation/report` | 违规通报 |
| 📍 轨迹回溯 | `pages/track/index` | 轨迹回溯 |
| 👤 我的 | `pages/profile/index` | 个人中心 |

#### 全部页面清单（19 个）

| 模块 | 页面路径 | 标题 | 说明 |
|------|----------|------|------|
| **认证** | `pages/auth/login` | 登录 | 密码登录 + 验证码登录 + 微信一键登录 |
| | `pages/auth/register` | 注册 | 手机号 + 验证码 + 用户协议勾选 |
| | `pages/auth/reset-password` | 重置密码 | 手机号 + 验证码重置 |
| **首页** | `pages/home/index` | 首页 | 仪表盘（统计卡片 + 快捷入口 + 最新违规） |
| **违规** | `pages/violation/report` | 违规通报 | 公开违规列表 + 侧滑筛选 + 下拉刷新 |
| | `pages/violation/my-violations` | 我的违规 | 个人违规记录列表 |
| | `pages/violation/detail` | 违规详情 | 完整违规信息 + 证据图片预览 + 申诉入口 |
| **轨迹** | `pages/track/index` | 轨迹查询 | 搜索 + 快捷入口 + 查询历史 |
| | `pages/track/playback` | 轨迹回放 | 地图轨迹动画播放（倍速控制 1x/2x/4x/8x） |
| | `pages/track/fence` | 电子围栏 | 地图展示围栏多边形 + 围栏列表 |
| | `pages/track/history` | 轨迹历史 | 日期筛选 + 轨迹记录列表 + 分页 |
| **申诉** | `pages/appeal/submit` | 提交申诉 | 申诉表单（原因/描述/证据上传） |
| | `pages/appeal/list` | 我的申诉 | 申诉记录列表 + 状态标签 |
| | `pages/appeal/detail` | 申诉详情 | 渐变色状态顶栏 + 证据展示 + 回复内容 |
| **个人** | `pages/profile/index` | 个人中心 | 用户卡片 + 功能菜单 + 资料编辑弹窗 |
| | `pages/profile/settings` | 设置 | 通知开关 + 清缓存 + 隐私/协议链接 |
| | `pages/profile/privacy` | 隐私政策 | 静态内容页 |
| | `pages/profile/user-agreement` | 用户协议 | 静态内容页 |
| | `pages/profile/feedback` | 意见反馈 | 反馈邮箱展示 |

### 5.4 设计风格

移动端采用**温暖自然**设计风格：

| 色彩 | 色值 | 用途 |
|------|------|------|
| 米白色 | `#FAF7F2` | 背景色 |
| 鼠尾草绿 | `#7D9E87` | 主色调 / 已处理状态 |
| 陶土红 | `#C9735D` | 警告 / 已拒绝状态 |
| 天空蓝 | `#6B9AC4` | 信息提示 |
| 暖杏色 | `#D4A574` | 待处理状态 |

---

## 六、三端交互关系图

```
┌──────────────────────────────────────────────────────────┐
│                       用户 / 管理员                       │
└────────────┬─────────────────────────────┬───────────────┘
             │                             │
     ┌───────▼───────┐           ┌─────────▼─────────┐
     │  frontend-web │           │  frontend-mobile   │
     │  (Vue 3 SPA)  │           │  (UniApp 小程序)    │
     │  :8081        │           │  微信 / H5 / App    │
     └───────┬───────┘           └─────────┬─────────┘
             │  HTTP / REST                 │  HTTP / REST
             │  Authorization: Bearer JWT   │  Authorization: Bearer JWT
             └──────────┬──────────────────┘
                        │
              ┌─────────▼─────────┐
              │     backend       │
              │  (Spring Boot)    │
              │  :8080/api        │
              └─────────┬─────────┘
                        │
              ┌─────────▼─────────┐
              │      MySQL        │
              │   scs_db          │
              │   (6 张业务表)     │
              └───────────────────┘
```

### 数据通信规范

| 项目 | 规范 |
|------|------|
| 通信协议 | HTTP REST |
| 数据格式 | JSON |
| 认证方式 | JWT Bearer Token（请求头 `Authorization`） |
| 统一响应体 | `{ code: number, message: string, data: any, timestamp: long }` |
| 分页格式 | MyBatis-Plus IPage（`records`, `total`, `current`, `size`） |
| 文件上传 | `multipart/form-data`，后端按日期分目录存储 |
| 错误处理 | 401 → 清除登录信息重定向登录页；业务异常 → 全局异常处理器统一包装 |

---

## 七、核心业务功能矩阵

| 功能模块 | 后端 API | Web 前端 | 移动端 |
|----------|---------|----------|--------|
| 用户登录 | ✅ `/auth/login` | ✅ Login.vue | ✅ auth/login |
| 用户注册 | ✅ `/auth/register` | ✅ Register.vue | ✅ auth/register |
| 密码重置 | ✅ `/auth/reset-password` | ✅ ResetPassword.vue | ✅ auth/reset-password |
| 短信验证码 | ✅ `/auth/sendCode` | ✅ 注册/重置页调用 | ✅ 注册/重置页调用 |
| 微信登录 | — | — | ✅ auth/login (wxLogin) |
| 违规列表 | ✅ `/violations/reports` | ✅ ViolationReport.vue | ✅ violation/report |
| 我的违规 | ✅ `/violations/my` | ✅ MyViolations.vue | ✅ violation/my-violations |
| 违规详情 | ✅ `/violations/{id}` | ✅ 对话框详情 | ✅ violation/detail |
| 违规统计 | ✅ `/violations/statistics` | ✅ 首页统计 | ✅ 首页统计卡片 |
| 添加/编辑违规 | ✅ (管理员) | ✅ ViolationReport.vue | — |
| 提交申诉 | ✅ `/appeals` | ✅ Appeal.vue (三步骤) | ✅ appeal/submit |
| 我的申诉 | ✅ `/appeals/my` | ✅ Appeal.vue | ✅ appeal/list |
| 申诉处理 | ✅ (管理员) | ✅ AppealComplaintHandler.vue | — |
| 轨迹查询 | ✅ `/tracks/byDate` | ✅ TrajectoryTracking.vue | ✅ track/playback |
| 轨迹历史 | ✅ `/tracks/list` | ✅ TrajectoryTracking.vue | ✅ track/history |
| 轨迹上传 | ✅ `/tracks/upload` | — | ✅ track API |
| 轨迹回放 | — | ✅ 高德地图轨迹 | ✅ track/playback (倍速) |
| 电子围栏 | ✅ `/fences` CRUD | ✅ TrajectoryTracking.vue | ✅ track/fence |
| 用户管理 | ✅ (管理员) | ✅ AdminManagement.vue | — |
| 个人信息 | ✅ `/users/profile` | ✅ UserProfile.vue | ✅ profile/index |
| 文件上传 | ✅ `/upload` | ✅ 证据/头像上传 | ✅ uni.uploadFile |
| 大屏监控 | ✅ `/screen/stats` | ✅ ViolationScreen.vue | — |

---

## 八、环境要求与启动方式

### 8.1 环境依赖

| 依赖项 | 最低版本 |
|--------|----------|
| JDK | 17+ |
| Maven | 3.6+ |
| Node.js | 16+ |
| MySQL | 8.0+ |
| HBuilderX | 最新版（UniApp 开发，可选） |

### 8.2 启动流程

**1. 数据库初始化**
```bash
# 创建数据库并执行初始化脚本
mysql -u root -p < backend/sql/init_schema.sql
mysql -u root -p scs_db < backend/add_admin.sql
mysql -u root -p scs_db < backend/init_test_data.sql
```

**2. 启动后端服务**
```bash
cd backend
# 修改 src/main/resources/application.yml 中的数据库连接配置
mvn spring-boot:run
# 服务启动于 http://localhost:8080/api
```

**3. 启动 Web 前端**
```bash
cd frontend-web
npm install
npm run serve
# 开发服务器启动于 http://localhost:8081
```

**4. 启动移动端**
```bash
cd frontend-mobile
npm install
# H5 开发模式
npm run dev:h5
# 微信小程序开发模式
npm run dev:mp-weixin
# 然后使用微信开发者工具打开 dist/dev/mp-weixin 目录
```

---

## 九、项目文件索引

| 文件 | 说明 |
|------|------|
| `README.md` | 项目总体介绍与快速启动指南 |
| `CONTRIBUTING.md` | 团队 Git 协作开发规范（分支管理、PR 流程） |
| `未实现功能清单.md` | 待开发功能详细清单与实现方案 |
| `后续手动操作指引.md` | 后续手动操作步骤指引 |
| `backend/构建后端文档.md` | 后端构建说明文档 |
| `backend/后端对前端的要求.md` | 后端接口对前端的数据契约要求 |
| `frontend-web/后端接口文档.md` | Web 端使用的后端接口文档 |
| `frontend-web/前端报告.md` | Web 前端开发报告 |
| `frontend-web/报告.md` | 综合报告 |
| `frontend-mobile/小程序项目文档.md` | 移动端小程序项目文档 |
| `frontend-mobile/后端对前端的要求.md` | 移动端接口对接要求 |
