# 项目未实现功能清单与实现方案

> 审查日期：2026年2月14日
> 涉及模块：backend（Spring Boot）、frontend-web（Vue 3）、frontend-mobile（UniApp 微信小程序）

---

## 〇、二次复审修订说明（本轮）

> 复审工具：GPT-5.3-Codex（结合代码仓库逐文件核对）

本版在原清单基础上做了事实纠偏与风险补充，后续实现以本版为准：

- 保留原结论中大部分“未实现”条目，但修正了少量“表述不准确”项（重点是 `1.8`、`2.5`、`2.6`）。
- 新增 3 个 **P0 联调阻断项**：
  - Web 违规筛选时间字段不一致（`startTime/endTime` vs `startDate/endDate`）
  - Web 大屏统计字段契约不一致（前端字段名与后端 VO 字段名不匹配）
  - 小程序 `AUTH` 端点常量缺失（`REGISTER/LOGOUT/REFRESH_TOKEN` 未定义）
- 本轮密码方案调整为：**仅新注册用户使用加密，不强制迁移历史密码**（历史数据迁移放到下一阶段）。

---

## 一、后端（backend）需要实现的功能

### 1.1 🔴 用户注册接口 `POST /auth/register`

**现状**：`WebMvcConfig.java` 已将 `/auth/register` 从 JWT 拦截中排除，但 `AuthController` 中无对应方法。

**实现方案**：
- 在 `AuthController` 中新增 `register` 方法
- 在 `AuthServiceImpl` 中实现注册逻辑：校验用户名唯一性 → 密码加密 → 插入 `sys_user` 表
- 新增 `RegisterDTO`（username, password, name, phone, email, department, userType）
- 返回 `Result.success("注册成功")`

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/controller/AuthController.java` — 新增方法
- `backend/src/main/java/org/example/sc_backend/service/AuthService.java` — 新增接口方法
- `backend/src/main/java/org/example/sc_backend/service/impl/AuthServiceImpl.java` — 实现注册逻辑
- `backend/src/main/java/org/example/sc_backend/dto/RegisterDTO.java` — 新建DTO

---

### 1.2 🔴 轨迹上传接口 `POST /tracks/upload`

**现状**：文档标记为高优先级，但完全未实现。小程序端的 `api/track.js` 已定义了 `uploadTrackPoints` 方法。

**实现方案**：
- 在 `TrackController` 新增 `uploadTrackPoints` 方法
- 接收参数：`userId`, `date`, `trackPoints`（JSON数组，每个点含 latitude, longitude, timestamp, speed）
- 逻辑：查找当天是否已有轨迹记录 → 有则追加点位 → 无则新建记录
- 计算 `startTime`, `endTime`, `distance`（根据轨迹点计算）
- 新增 `TrackUploadDTO`

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/controller/TrackController.java`
- `backend/src/main/java/org/example/sc_backend/service/TrackService.java`
- `backend/src/main/java/org/example/sc_backend/service/impl/TrackServiceImpl.java`
- `backend/src/main/java/org/example/sc_backend/dto/TrackUploadDTO.java` — 新建

---

### 1.3 🔴 用户管理接口（`UserController`）

**现状**：Web 前端 `AdminManagement.vue` 完全使用硬编码数据，后端无任何用户管理端点。

**实现方案**：
- 新建 `UserController`，提供以下端点：
  - `GET /users/list` — 分页查询用户列表（支持 keyword/userType/status 筛选）
  - `GET /users/{id}` — 获取用户详情
  - `PUT /users/{id}` — 更新用户信息
  - `PUT /users/{id}/status` — 启用/禁用用户
  - `GET /users/search` — 搜索用户（供轨迹查询选人用）
- 新建 `UserService` 和 `UserServiceImpl`
- 新增 `UserQueryDTO`

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/controller/UserController.java` — 新建
- `backend/src/main/java/org/example/sc_backend/service/UserService.java` — 新建
- `backend/src/main/java/org/example/sc_backend/service/impl/UserServiceImpl.java` — 新建
- `backend/src/main/java/org/example/sc_backend/dto/UserQueryDTO.java` — 新建

---

### 1.4 🟡 `/violations/my` 增加筛选参数

**现状**：`ViolationController.getMyViolations()` 仅接受 `page` 和 `pageSize`，缺少 `type/status/startTime/endTime` 筛选。

**实现方案**：
- 在 Controller 方法中增加 `@RequestParam` 参数：`type`, `status`, `startTime`, `endTime`
- 在 Service 层 `getMyViolations` 方法的 `LambdaQueryWrapper` 中增加条件：
  - `wrapper.eq(StringUtils.isNotBlank(type), BizViolation::getType, type)`
  - `wrapper.eq(StringUtils.isNotBlank(status), BizViolation::getStatus, status)`
  - `wrapper.ge(startTime != null, BizViolation::getViolationTime, startTime)`
  - `wrapper.le(endTime != null, BizViolation::getViolationTime, endTime)`

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/controller/ViolationController.java`
- `backend/src/main/java/org/example/sc_backend/service/ViolationService.java`
- `backend/src/main/java/org/example/sc_backend/service/impl/ViolationServiceImpl.java`

---

### 1.5 🟡 `/violations/reports` 日期范围筛选修复

**现状**：`ViolationQueryDTO` 中已定义 `startDate`/`endDate` 字段，但 `ViolationServiceImpl.getViolationPage()` 方法构建查询条件时**未使用这两个字段**。

**实现方案**：
- 在 `ViolationServiceImpl.getViolationPage()` 中追加：
  - `wrapper.ge(dto.getStartDate() != null, BizViolation::getViolationTime, dto.getStartDate())`
  - `wrapper.le(dto.getEndDate() != null, BizViolation::getViolationTime, dto.getEndDate())`

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/service/impl/ViolationServiceImpl.java`

---

### 1.6 🟡 上传文件静态资源映射

**现状**：`FileController` 将文件保存到 `./uploads` 目录，但未配置静态资源映射，文件 URL 无法通过 HTTP 访问。

**实现方案**：
- 在 `WebMvcConfig` 的 `addResourceHandlers` 方法中增加资源映射：
  ```
  registry.addResourceHandler("/uploads/**")
          .addResourceLocations("file:./uploads/");
  ```
- 同时将 `/uploads/**` 路径排除出 JWT 拦截器

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/common/config/WebMvcConfig.java`

---

### 1.7 🟡 违规统计接口 `GET /violations/statistics`

**现状**：小程序首页调用 `violationApi.getStatistics()` 获取统计数据（总违规/我的违规/待处理），后端无此接口。

**实现方案**：
- 在 `ViolationController` 新增 `getStatistics` 方法
- 返回：`totalCount`, `myCount`(当前用户), `pendingCount`

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/controller/ViolationController.java`
- `backend/src/main/java/org/example/sc_backend/service/ViolationService.java`
- `backend/src/main/java/org/example/sc_backend/service/impl/ViolationServiceImpl.java`

---

### 1.8 🟡 违规通报列表接口（面向小程序）

**现状**：小程序 `report.vue` 和 `home/index.vue` 调用 `violationApi.getViolations()`，但 `violation.js` 中无此方法；后端已有 `/violations/reports`，当前并未做严格管理员角色拦截。

**实现方案**：
- 在小程序端 `api/violation.js` 中新增 `getViolations()` 方法，调用 `/violations/reports`
- 若后续补齐管理员权限控制（见 1.10），需同步提供小程序可访问的公开/用户级违规通报端点

**涉及文件**：
- `frontend-mobile/src/api/violation.js` — 新增方法
- （可选）`backend/src/main/java/org/example/sc_backend/controller/ViolationController.java`

---

### 1.9 🟢 密码加密（BCrypt）

**现状**：`AuthServiceImpl` 中登录使用 `password.equals(admin.getPassword())` 明文比对。

**实现方案**：
- 在 `pom.xml` 中确认 Spring Security 依赖（或手动添加 BCrypt 工具类）
- 注册时使用 `BCryptPasswordEncoder.encode()` 加密
- 登录时使用 `BCryptPasswordEncoder.matches()` 验证
- 本轮先落地“新注册用户加密 + 登录兼容校验”，全量历史密码迁移放到下一阶段

**⚠️ 后续需要你操作（下一阶段）**：如切换为全量加密，需要执行 SQL 更新现有用户密码：
```sql
-- 需要用 Java 代码生成加密后的密码并更新，示例：
-- UPDATE sys_user SET password = '$2a$10$...' WHERE id = ...;
-- UPDATE sys_admin SET password = '$2a$10$...' WHERE id = ...;
```

**涉及文件**：
- `backend/pom.xml`（如需添加依赖）
- `backend/src/main/java/org/example/sc_backend/service/impl/AuthServiceImpl.java`

---

### 1.10 🟢 管理员权限校验

**现状**：所有管理员端点（添加/更新违规、处理申诉、围栏增删改）均无角色校验。

**实现方案**：
- 新建 `@AdminOnly` 自定义注解 + `AdminInterceptor` 拦截器
- 或在各 Controller 方法中手动从 JWT 解析 role 并判断
- 管理员接口列表：`POST/PUT /violations`, `GET /appeals/list`, `PUT /appeals/{id}/handle`, `POST/PUT/DELETE /fences`

**涉及文件**：
- 新建拦截器或注解
- 涉及的各 Controller 文件

---

## 二、Web 前端（frontend-web）需要实现的功能

### 2.1 🔴 Register.vue — 注册功能实现

**现状**：UI 完整但 `handleRegister()` 仅 `console.log`，无 API 调用。

**实现方案**：
- 在 `api/auth.js` 中新增 `register(data)` 函数，POST 到 `/auth/register`
- 在 `Register.vue` 中：
  - 添加 el-form 验证规则（用户名、密码、确认密码、手机号）
  - `handleRegister()` 调用 `register()` API
  - 成功后跳转到登录页
- 验证码功能（如后端不实现短信服务，可先去除验证码输入框或标记"暂不支持"）

**涉及文件**：
- `frontend-web/src/api/auth.js`
- `frontend-web/src/views/Register.vue`

---

### 2.2 🔴 ResetPassword.vue — 重置密码功能实现

**现状**：UI 完整但 `handleReset()` 仅 `console.log`，无 API 调用。

**实现方案**：
- 后端需新增 `POST /auth/reset-password` 接口
- 在 `api/auth.js` 中新增 `resetPassword(data)` 函数
- 在 `ResetPassword.vue` 中实现表单验证和 API 调用
- 成功后跳转到登录页

**涉及文件**：
- `backend/src/main/java/org/example/sc_backend/controller/AuthController.java` — 新增接口
- `frontend-web/src/api/auth.js` — 新增方法
- `frontend-web/src/views/ResetPassword.vue` — 完善逻辑

---

### 2.3 🔴 AdminManagement.vue — 用户管理对接后端

**现状**：完全使用硬编码静态数据，所有 CRUD 操作仅修改前端 ref，刷新即丢失。

**实现方案**：
- 新建 `api/user.js`，定义以下 API 函数：
  - `getUserList(params)` — GET `/users/list`
  - `getUserDetail(id)` — GET `/users/{id}`
  - `updateUser(id, data)` — PUT `/users/{id}`
  - `updateUserStatus(id, status)` — PUT `/users/{id}/status`
- 在 `AdminManagement.vue` 中：
  - `onMounted` 调用 `getUserList()` 替代硬编码数据
  - 搜索/筛选调用后端分页接口
  - 编辑/禁用操作调用对应 API
  - 分页使用服务端分页

**涉及文件**：
- `frontend-web/src/api/user.js` — 新建
- `frontend-web/src/components/AdminManagement.vue` — 重写数据获取和操作逻辑

---

### 2.4 🔴 UserProfile.vue — 个人中心对接后端

**现状**：所有数据硬编码（"张三"、"2023123456"等），编辑保存仅修改本地 ref，退出登录不清除 token。

**实现方案**：
- `onMounted` 时从 `localStorage.getItem('userInfo')` 读取登录用户信息并回显
- 或调用 `getUserInfo()` API（已在 `api/auth.js` 中定义但从未使用）刷新用户信息
- 头像上传：调用 `uploadFile()` 上传到服务端，获取 URL 后更新用户信息
- 编辑保存：需要后端 `PUT /users/profile` 接口（个人资料修改）
- 退出登录：调用 `localStorage.removeItem('token')`, `localStorage.removeItem('userInfo')`, `localStorage.removeItem('isLoggedIn')` 后再跳转
- 违规统计：调用违规统计 API 获取真实数据

**涉及文件**：
- `frontend-web/src/components/UserProfile.vue`
- `frontend-web/src/api/auth.js`（新增 `updateProfile` 方法）
- 后端需新增 `PUT /auth/profile` 或 `PUT /users/profile` 接口

---

### 2.5 🔴 TrajectoryTracking.vue — 地图集成

**现状**：地图区域仍为占位符，用户列表为硬编码假用户；但页面中已存在部分 API 对接代码，属于“部分对接、未完成闭环”。

**实现方案**：
- 集成高德地图（项目已安装 `@amap/amap-jsapi-loader`）：
  - 在地图容器中初始化 AMap 实例
  - 轨迹查询后在地图上绘制 `Polyline`（轨迹线）+ `Marker`（起终点）
  - 围栏用 `Polygon` 在地图上绘制
  - 添加地图工具栏（缩放、定位）
- 用户搜索：调用后端 `GET /users/search` 接口替代硬编码列表
- 围栏查看对话框：使用 API 返回的动态数据替代硬编码文本
- 同步修复围栏数据结构映射（前端读取分页 `records` 与后端返回 `List` 的差异）以及字段名映射（如 `description`/`rangeDesc`）

**⚠️ 需要你操作**：需要到高德开放平台申请 Web 端 JS API Key，并配置到项目中。

**涉及文件**：
- `frontend-web/src/components/TrajectoryTracking.vue` — 大幅重写地图部分

---

### 2.6 🟡 ViolationScreen.vue — 图表数据更新修复

**现状**：调用 `getScreenStats()` 获取后端数据后存入 ref 变量，但 ECharts 图表仍使用硬编码数据初始化，**未重新调用 `setOption` 更新图表**；同时存在前后端字段命名契约不一致。

**实现方案**：
- 在 `fetchScreenStats()` 获取数据成功后，对每个图表 chart 实例调用 `setOption()` 更新：
  - 饼图：用 `typeDistribution` 数据更新
  - 折线图：用 `dailyTrend` 数据更新
  - 柱状图：用 `areaRanking` 数据更新
  - 地图散点：用 `violationPoints` 更新
- 将 `initCharts()` 拆分：初始化图表实例 + 更新数据分开处理
- 统一统计字段契约：前端字段（`todayViolations/typeDistribution/...`）与后端字段（`todayCount/typeStats/...`）必须做兼容映射或统一命名

**涉及文件**：
- `frontend-web/src/components/ViolationScreen.vue`

---

### 2.7 🟡 退出登录修复

**现状**：`UserProfile.vue` 的 `logout()` 仅做路由跳转，未清除 localStorage 中的认证状态。

**实现方案**：
- 清除 `localStorage` 中所有认证相关数据：`token`, `userInfo`, `isLoggedIn`
- 然后 `router.push('/')`

**涉及文件**：
- `frontend-web/src/components/UserProfile.vue`

---

### 2.8 🟡 AppealComplaintHandler.vue — 批量处理功能

**现状**：`batchProcess()` 方法显示 "批量处理功能暂未实现"。

**实现方案**：
- 添加列表多选功能（checkbox）
- 选中后批量调用 `handleAppeal()` 接口处理
- 或后端新增批量处理接口 `PUT /appeals/batch-handle`

**涉及文件**：
- `frontend-web/src/components/AppealComplaintHandler.vue`
- （可选）后端新增批量处理端点

---

## 三、移动端（frontend-mobile）需要实现的功能

### 3.1 🔴 4 个已开发页面注册路由

**现状**：以下页面代码已完成但**未在 `pages.json` 中注册路由**，无法访问：
- `pages/appeal/submit` — 申诉提交
- `pages/appeal/list` — 申诉列表
- `pages/appeal/detail` — 申诉详情
- `pages/track/history` — 轨迹历史

**实现方案**：
在 `pages.json` 的 `pages` 数组中添加 4 条路由配置：
```json
{
  "path": "pages/appeal/submit",
  "style": { "navigationBarTitleText": "提交申诉" }
},
{
  "path": "pages/appeal/list",
  "style": { "navigationBarTitleText": "我的申诉" }
},
{
  "path": "pages/appeal/detail",
  "style": { "navigationBarTitleText": "申诉详情" }
},
{
  "path": "pages/track/history",
  "style": { "navigationBarTitleText": "轨迹历史" }
}
```

**涉及文件**：
- `frontend-mobile/src/pages.json`

---

### 3.2 🔴 violation.js 补充缺失的 API 方法

**现状**：`violation.js` 仅有 `getMyViolations` 和 `getViolationDetail`，但多处页面调用了不存在的方法。

**实现方案**：
在 `api/violation.js` 中新增：
- `getViolations(params)` — 获取违规通报列表（供 `report.vue`、`home/index.vue` 使用），调用 `GET /violations/reports`
- `getStatistics()` — 获取违规统计（供 `home/index.vue` 使用），调用 `GET /violations/statistics`
- 并统一分页返回解析（`list` vs `records`）以匹配后端 MyBatis-Plus 分页结构

**涉及文件**：
- `frontend-mobile/src/api/violation.js`

---

### 3.3 🔴 track.js 补充缺失的 API 方法

**现状**：`playback.vue` 调用 `trackApi.getHistory()`，但 `track.js` 中无此方法。

**实现方案**：
在 `api/track.js` 中新增 `getHistory(params)` 方法，调用 `GET /tracks/byDate`。

> 说明：当前后端 `byDate` 更偏向“按用户/日期”查询，若前端按“车牌号”查询，需要补充后端查询维度或在前端改为已支持参数。

**涉及文件**：
- `frontend-mobile/src/api/track.js`

---

### 3.4 🟡 登录页验证码模式字段修复

**现状**：验证码登录模式下引用 `form.value.phone`，但表单对象中只定义了 `username` 字段。

**实现方案**：
- 在 `form` 对象中添加 `phone` 字段
- 或将验证码模式的 `sendCode()` 中的 `form.value.phone` 改为 `form.value.username`

**涉及文件**：
- `frontend-mobile/src/pages/auth/login.vue`

---

### 3.5 🟡 轨迹回放日期选择器实现

**现状**：`showDatePicker()` 仅弹出 "日期选择器开发中" toast。

**实现方案**：
- 使用 `uni.showDatePicker` 或自定义 picker 组件实现日期选择
- 选中日期后调用 `trackApi.getTrackByDate()` 更新轨迹数据

**涉及文件**：
- `frontend-mobile/src/pages/track/playback.vue`

---

### 3.6 🟡 个人中心资料编辑功能

**现状**：编辑弹窗保存按钮永远为 `btn-disabled` 状态，表单无 v-model 绑定、无提交逻辑。

**实现方案**：
- 为编辑表单各字段添加 v-model 绑定
- 实现变更检测，有修改时启用保存按钮
- 保存时调用后端 `PUT /auth/profile` 更新用户信息
- 同步更新本地 Store 中的用户信息

**涉及文件**：
- `frontend-mobile/src/pages/profile/index.vue`

---

### 3.7 🟡 violation store 修复

**现状**：`stores/violation.js` 中 `fetchPublicViolations()` 调用了不存在的 `violationApi.getViolations()`。

**实现方案**：
- 在 `api/violation.js` 新增 `getViolations()` 后（见 3.2），此问题自动修复

**涉及文件**：
- `frontend-mobile/src/stores/violation.js`（无需改动，修复 API 即可）

---

### 3.8 🟢 reset-password.vue 配色方案统一

**现状**：使用旧配色（`#F5F5F5`/`#007AFF`），与全局温暖自然风格不一致。

**实现方案**：
- 将背景色改为 `#FAF7F2`
- 按钮主色改为 `#7D9E87`（鼠尾草绿）
- 统一圆角、阴影等样式变量

**涉及文件**：
- `frontend-mobile/src/pages/auth/reset-password.vue`

---

### 3.9 🔴 小程序 AUTH 端点常量补全（新增）

**现状**：`frontend-mobile/src/api/auth.js` 中已使用 `API_ENDPOINTS.AUTH.REGISTER / LOGOUT / REFRESH_TOKEN`，但 `frontend-mobile/src/config/api.js` 未定义这些常量，运行时会出现 `undefined` 接口地址。

**实现方案**：
- 在 `frontend-mobile/src/config/api.js` 的 `AUTH` 节点补充：
  - `REGISTER`
  - `LOGOUT`
  - `REFRESH_TOKEN`
- 与后端现有/计划接口路径保持一致（建议同步到接口文档）

**涉及文件**：
- `frontend-mobile/src/config/api.js`
- `frontend-mobile/src/api/auth.js`（联调校验）

---

## 四、需要你手动操作的事项

### 4.1 数据库操作

本轮策略：先实现“新注册用户加密”，历史数据暂不强制迁移。

后续若切换为全量加密，需要你执行数据库迁移（DML）：
- `sys_user.password` 明文 → BCrypt 哈希
- `sys_admin.password` 明文 → BCrypt 哈希

建议迁移步骤：
1. 备份原表
2. 批量更新密码哈希
3. 抽样验证登录
4. 保留回滚脚本

### 4.2 高德地图 Key

Web 前端地图集成（2.5）需要高德开放平台的 JS API Key：
1. 访问 https://lbs.amap.com/
2. 注册/登录 → 控制台 → 应用管理 → 创建新应用
3. 添加 Key（服务平台选"Web端(JS API)"）
4. 将 Key 配置到项目中

### 4.3 微信小程序 AppID（如需微信登录）

后端微信登录接口（1.2 文档要求）需要：
1. 微信公众平台注册小程序
2. 获取 AppID 和 AppSecret
3. 配置到后端 `application.yml`

### 4.4 接口命名口径确认（新增）

为避免联调反复失败，你需要确认并固化以下命名口径（文档与代码一致）：
- `reset-password` vs `resetPassword`
- `/upload` vs `/upload/file`
- `/appeals` vs `/appeals/submit`
- 分页字段 `list` vs `records`

---

## 五、实施优先级建议

| 优先级 | 编号 | 功能 | 工作量估计 |
|--------|------|------|-----------|
| P0 | 新增 | Web 违规筛选时间字段对齐（`startTime/endTime` ↔ `startDate/endDate`） | 10-20 分钟 |
| P0 | 新增 | Web 大屏统计字段契约对齐（前后端命名映射） | 20-30 分钟 |
| P0 | 3.9 | 小程序 AUTH 端点常量补全 | 10 分钟 |
| P0 | 3.1 | 小程序 4 个页面注册路由 | 5 分钟 |
| P0 | 3.2 | 小程序 violation.js 补充方法 | 15 分钟 |
| P0 | 3.3 | 小程序 track.js 补充方法 | 10 分钟 |
| P0 | 1.6 | 后端上传文件静态资源映射 | 10 分钟 |
| P1 | 1.1 | 后端注册接口 | 30 分钟 |
| P1 | 2.1 | Web 注册功能实现 | 30 分钟 |
| P1 | 1.3 | 后端用户管理接口 | 1 小时 |
| P1 | 2.3 | Web 用户管理对接 | 1 小时 |
| P1 | 2.4 | Web 个人中心对接 | 45 分钟 |
| P1 | 2.6 | Web 大屏图表修复 | 30 分钟 |
| P1 | 2.7 | Web 退出登录修复 | 5 分钟 |
| P1 | 1.4 | 后端 /violations/my 筛选 | 20 分钟 |
| P1 | 1.5 | 后端日期范围筛选修复 | 10 分钟 |
| P1 | 1.7 | 后端违规统计接口 | 20 分钟 |
| P2 | 1.2 | 后端轨迹上传接口 | 45 分钟 |
| P2 | 2.2 | Web 重置密码功能 | 30 分钟 |
| P2 | 2.5 | Web 地图集成 | 2-3 小时 |
| P2 | 3.4 | 小程序登录验证码字段修复 | 10 分钟 |
| P2 | 3.5 | 小程序轨迹回放日期选择器 | 30 分钟 |
| P2 | 3.6 | 小程序个人资料编辑 | 30 分钟 |
| P3 | 1.9 | 密码加密 | 30 分钟 |
| P3 | 1.10 | 管理员权限校验 | 45 分钟 |
| P3 | 2.8 | 批量处理功能 | 30 分钟 |
| P3 | 3.8 | 小程序配色统一 | 15 分钟 |

---

## 六、验证方式

完成所有修改后，按以下步骤验证：

1. **后端**：启动 Spring Boot，使用 Postman/curl 验证所有新增和修复的 API 端点
2. **Web 前端**：`npm run serve` 启动，依次测试注册→登录→各功能模块→退出
3. **小程序**：微信开发者工具中编译运行，测试申诉流程、轨迹历史、违规通报
4. **关键测试点**：
   - 注册新用户 → 登录 → 查看个人中心（应显示真实信息）
   - 管理员登录 → 用户管理（应从后端加载真实用户列表）
   - 违规大屏（ECharts 图表应显示后端返回的真实数据）
   - 文件上传后通过返回的 URL 能在浏览器中访问到文件
   - 小程序进入"我的申诉"页面（原本无法访问）