# 后续手动操作指引

> 更新时间：2026-02-14
> 适用范围：`screen_track_assessment`（backend + frontend-web + frontend-mobile）

## 1. 必做：配置高德地图 Key（Web 轨迹地图）

`frontend-web` 的轨迹地图已完成代码接入，但需要你提供高德 Web Key 才能真正显示地图。

### 1.1 申请 Key
1. 打开高德开放平台：https://lbs.amap.com/
2. 登录后进入控制台，创建应用
3. 添加 Key，服务平台选择「Web端（JS API）」
4. 在安全设置里配置允许访问域名（本地调试一般是 `localhost`、`127.0.0.1` 等）

### 1.2 在项目中配置 Key（任选其一）
- 方式 A（推荐）：在 `frontend-web` 环境变量中配置 `VUE_APP_AMAP_KEY`
- 方式 B（临时调试）：在浏览器控制台执行
  ```js
  localStorage.setItem('AMAP_WEB_KEY', '你的key')
  ```

### 1.3 验证
1. 启动 `frontend-web`
2. 登录后进入「轨迹回溯」
3. 选择日期后应能看到：底图 + 轨迹线 + 起终点 + 围栏（如围栏有坐标）

---

## 2. 建议：确认前后端接口口径（避免联调反复）

请确认以下约定并固定到接口文档：
- 分页字段统一：`records`（后端 MyBatis-Plus）
- 日期筛选字段：`startDate/endDate`（Web 已做兼容）
- 认证路径：`/auth/reset-password`、`/auth/refresh-token`、`/auth/sendCode`、`/auth/logout`
- 围栏字段：`rangeDesc`、`coordinates`

---

## 3. 可选：历史密码全量迁移（安全加固）

当前已实现“新注册/重置使用 BCrypt + 历史明文登录自动升级”。

若你要一次性完成全量迁移，建议流程：
1. 备份 `sys_user`、`sys_admin`
2. 生成每条旧密码对应的 BCrypt 哈希
3. 批量更新密码字段
4. 抽样验证登录
5. 保留回滚脚本

> 注意：BCrypt 每次加密结果不同，不能用固定哈希覆盖所有用户。

---

## 4. 可选：微信登录能力（小程序）

若需要真实微信登录：
1. 在微信公众平台注册小程序
2. 获取 `AppID`/`AppSecret`
3. 后端实现并接入 `/auth/wechat/login`
4. 在 `application.yml` 中配置微信参数

---

## 5. 本轮已补齐的关键点（便于你验收）

- Web 轨迹页面：真实高德地图集成、轨迹/围栏绘制、管理员搜索用户查看轨迹
- 后端轨迹查询：`GET /tracks/byDate` 支持管理员按 `userId` 查询
- 后端权限收口：`/users/list`、`/users/{id}`、`/users/{id}/status`、`/users/search` 已加管理员校验
- 认证接口补齐：`/auth/sendCode`、`/auth/refresh-token`、`/auth/logout`
- 小程序注册联调修复：注册成功后跳回登录，不再错误假设“注册即返回 token+userInfo”

---

## 6. 你可以直接按这个顺序执行

1. 配高德 Key（第 1 节）
2. 跑一遍 Web 轨迹回溯联调
3. 确认接口口径（第 2 节）
4. 决定是否做全量密码迁移（第 3 节）
5. 决定是否做微信登录（第 4 节）
